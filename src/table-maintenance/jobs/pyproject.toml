[project]
name = "jobs"
version = "0.1.0"
description = "Iceberg table maintenance jobs (expire_snapshots, remove_orphan_files, rewrite_data_files, rewrite_manifests)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12.5",
    "pydantic-settings>=2.0",
]
