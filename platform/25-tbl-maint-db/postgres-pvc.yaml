apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tbl-maint-db
  namespace: default
  labels:
    app: tbl-maint-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
