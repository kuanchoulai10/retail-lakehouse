apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: polaris-cdc-role
  namespace: kafka-cdc
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["polaris-cdc-secret"]
  verbs: ["get"]
