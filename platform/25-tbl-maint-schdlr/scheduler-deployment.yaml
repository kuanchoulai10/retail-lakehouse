apiVersion: apps/v1
kind: Deployment
metadata:
  name: table-maintenance-scheduler
  namespace: default
  labels:
    app: table-maintenance-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: table-maintenance-scheduler
  template:
    metadata:
      labels:
        app: table-maintenance-scheduler
    spec:
      containers:
        - name: scheduler
          image: localhost:5001/table-maintenance-backend:20260424-233832
          imagePullPolicy: Never
          env:
            - name: GLAC_COMPONENT
              value: "scheduler"
            - name: GLAC_DATABASE_BACKEND
              value: "postgres"
            - name: GLAC_POSTGRES__DB_URL
              value: "postgresql+psycopg://tm:tm@tbl-maint-db.default.svc:5432/table_maintenance"
            - name: GLAC_SCHEDULER__INTERVAL_SECONDS
              value: "30"
