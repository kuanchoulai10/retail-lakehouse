apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: root-app
  namespace: argocd
  # finalizers:
  #   - argocd.argoproj.io/resources-finalizer
spec:
  project: default
  sources:
    - repoURL: https://github.com/kuanchoulai10/retail-lakehouse
      path: charts/app-of-apps
      targetRevision: main
      helm:
        releaseName: retail-lakehouse
        valuesObject:
          project:
            name: retail-lakehouse
          spec:
            destination:
              name: in-cluster
  destination:
    name: in-cluster
    namespace: argocd
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
